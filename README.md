# CIFFAR10
This project implements a Convolutional Neural Network (CNN) using PyTorch for image classification. The model is trained and evaluated on an image dataset (e.g., CIFAR-10) to demonstrate deep learning concepts including forward propagation, backpropagation, and model evaluation.
